function expoente(base, expoente) {
    // Método antigo resultado = Math.pow(base, expoente)
    // Método Novo abaixo
    let resultado = base ** expoente
    return resultado
}

console.log(expoente(2, 3))